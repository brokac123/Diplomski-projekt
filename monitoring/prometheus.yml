global:
  scrape_interval: 5s
  evaluation_interval: 5s
  external_labels:
    environment: 'local'

scrape_configs:
  - job_name: "fastapi"
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]

  - job_name: "node"
    static_configs:
      - targets: ["node-exporter:9100"]