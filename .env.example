# --- Database ---
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=booking_db
DATABASE_URL=postgresql://user:password@db/booking_db

# --- API Workers ---
# Change this value and run: docker compose up -d
# Options: 1, 2, 4
WORKERS=1
